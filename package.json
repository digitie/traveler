{
  "name": "recurring-date-patterns",
  "version": "0.1.0",
  "description": "A library for resolving recurring date patterns like 'every 3rd Monday' or 'every 2nd and 4th Sunday'",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "test:watch": "jest --watch"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.3",
    "typescript": "^5.4.5"
  }
}
